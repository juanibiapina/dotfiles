software:
  - gg (basher)
  - tmuxinator (gem)
  - bats (manual clone)

vim:
  - xml

rbenv:
  - deactivate if it doesn't exist

ideas:
  - per host dotfiles

bugs:
  - git status not being updated properly unless I run git status first

pkgname='delta'
pkgdesc='A syntax highlighter for git and diff output'
pkgver='0.0.18'
pkgrel='1'
arch=('x86_64')
url='https://github.com/dandavison/delta'
license=('MIT')
depends=()
optdepends=()
makedepends=()
checkdepends=()
provides=('delta')
conflicts=()
source=("${pkgname}-v${pkgver}.tar.gz::https://github.com/dandavison/delta/releases/download/${pkgver}/${pkgname}-${pkgver}-x86_64-unknown-linux-gnu.tar.gz")
sha256sums=('ff5597fd7bb8f9e8ecaefea4981623ae1847c32fe5ad537ec21a1777f3b4a828')

package() {
    install -Dm755 "${pkgname}-${pkgver}-x86_64-unknown-linux-gnu/delta" "$pkgdir/usr/bin/delta"
}

pkgname='sub'
pkgdesc='Organize groups of scripts into documented CLIs with subcommands'
pkgver='0.6.0'
pkgrel='1'
arch=('x86_64')
url='https://github.com/juanibiapina/sub'
license=('MIT')
depends=()
optdepends=()
makedepends=()
checkdepends=()
provides=('sub')
conflicts=()
source=("${pkgname}-v${pkgver}.tar.gz::https://github.com/juanibiapina/sub/releases/download/v${pkgver}/${pkgname}-v${pkgver}-x86_64-unknown-linux-gnu.tar.gz")
sha256sums=('a91575719ad9446594c477db58dd0396e8ec7c72238f993ccb03b6ea11387dc7')

package() {
    install -Dm755 "sub" "$pkgdir/usr/bin/sub"
}

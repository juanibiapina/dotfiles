# Instructions for AI Coding Agents

## Installing macOS software with Homebrew

- **Shared across all Macs**: Add brews/casks to `nix/modules/macos/system.nix`
- **Host-specific**: Add to the host's `configuration.nix` (e.g., `nix/hosts/macm1/configuration.nix`)

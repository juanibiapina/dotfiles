# Quicktask v1.2
# vim: ft=quicktask

SOFTWARE:
  - gg (basher)
  - tmuxinator (gem)
  - ruby and rubyges (on linux, apt-get)
  - bats (manual clone)
  - vim (ubuntu comes only with vi)

RBENV:
  - deactivate if it doesn't exist

DONE:
  - per host dotfiles
    @ DONE [Fri 2013-09-20]

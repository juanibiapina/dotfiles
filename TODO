# Quicktask v1.2
# vim: ft=quicktask

software:
  - gg (basher)
  - tmuxinator (gem)
  - ruby and rubyges (on linux, apt-get)
  - bats (manual clone)
  - vim (ubuntu comes only with vi)

rbenv:
  - deactivate if it doesn't exist

ideas:
  - per host dotfiles

done:

#!/usr/bin/env bash
#
# Summary: Switch NixOS to new config
#
# Usage: {cmd}

set -e

if [ "$(uname -s)" = "Darwin" ]; then
  darwin-rebuild switch --flake $DOTFILES_HOME/assets/nix
else
  sudo nixos-rebuild switch
fi
